[tool.poetry]
name = "music-library-tools"
version = "0.1.0"
description = ""
authors = ["Sasan Jacob Rasti <sasan.accounts@icloud.com>"]

[tool.poetry.dependencies]
python = "^3.7"
mutagen = "^1.45.1"
requests = "^2.24.0"
schedule = "^0.6.0"
pathvalidate = "^2.3.0"

[tool.poetry.dev-dependencies]
flake8 = "^3.8.4"
black = "^20.8b1"
mypy = "^0.790"
pytest = "^6.1.2"

[tool.poetry.scripts]
start = "music_library_tools.daemons:run"

[tool.black]
line-length = 130

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
